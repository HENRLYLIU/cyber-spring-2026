# 自动化测试流程 PRD (产品需求文档)

## 1. 项目背景

项目 `spring_festival` 是一个基于 Vite + React 的现代化 Web 应用。为了保证代码质量、提高开发效率并降低回归风险，需要搭建一套完整的自动化测试流程，并能够自动生成可视化测试报告。

## 2. 目标

- 建立单元测试和组件测试的基础设施。
- 实现测试执行的自动化。
- 提供易于阅读的 HTML 格式测试报告。
- 支持代码覆盖率统计。

## 3. 技术选型

- **测试框架**: [Vitest](https://vitest.dev/) (Vite 原生支持，速度快，配置简单)。
- **组件测试**: React Testing Library (@testing-library/react)。
- **环境模拟**: jsdom (模拟浏览器环境)。
- **报告生成**: Vitest 内置的 HTML reporter。
- **覆盖率工具**: @vitest/coverage-v8。

## 4. 自动化测试流程设计

### 4.1 测试分类

1.  **单元测试 (Unit Tests)**: 针对纯逻辑函数、工具类进行的测试。
2.  **组件测试 (Component Tests)**: 针对 React 组件的渲染、交互、状态变化进行的测试。

### 4.2 执行流

1.  **本地开发环境**:
    - 开发者运行 `npm run test` 进行交互式测试。
    - 运行 `npm run test:ui` 开启可视化测试界面。
2.  **持续集成 (建议后续集成)**:
    - 在 Git Push 或 PR 时触发 GitHub Actions 运行测试。
3.  **报告产出**:
    - 生成 `html` 格式的测试结果报告。
    - 生成 `coverage` 覆盖率报告。

## 5. 功能需求清单

| 编号 | 功能点   | 描述                               | 优先级 |
| :--- | :------- | :--------------------------------- | :----- |
| F1   | 环境搭建 | 安装并配置 Vitest, RTL, jsdom      | P0     |
| F2   | 测试脚本 | 在 package.json 中集成测试命令     | P0     |
| F3   | 示例用例 | 编写第一个单元测试和组件测试用例   | P0     |
| F4   | 报告生成 | 配置同步输出 HTML 报告和覆盖率文件 | P1     |
| F5   | UI 界面  | 支持图形化展示测试过程 (Vitest UI) | P2     |

## 6. 验收标准

1.  运行 `npm run test` 能够正确识别并执行匹配后缀为 `.test.tsx` 或 `.spec.tsx` 的文件。
2.  测试完成后，在项目根目录下生成 `html-report/` 文件夹，打开其中的 `index.html` 可看到测试详情。
3.  生成代码覆盖率报告，明确显示各模块的覆盖比例。
